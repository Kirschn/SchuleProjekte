import GLOOP.*;
GLOOP_object_generator {
    public boolean createBlockFromObject(object Obj, generalMapScale) {
            long posX = Obj.getInt("posX") * generalMapScale;
            long posY = Obj.getInt("posY") * generalMapScale;

        }
}